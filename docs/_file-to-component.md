File names will be lowercased and converted to hyphened names by replacing `/` and `_` to `-` and removing extensions. Examples:
* folder/file-name.vue -> `<folder-file-name>`
* folder/fileName.js -> `<folder-filename>`